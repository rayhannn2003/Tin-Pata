# PM2 / process manager (socket files break EAS tarball)
.pm2
**/*.sock

# Generated native projects — EAS Build runs prebuild from app.json
android/
ios/

# Build caches (when android/ exists locally after prebuild)
android/build
android/app/build
android/.gradle
ios/build
ios/Pods

# Dependencies & caches
node_modules
.expo
